/* global define */

define(
    [
        'angular'
    ],
    function MenuMainModel(angular) {
        'use strict';

        return angular.module('MenuMainModel', []).factory(
            'MenuMainModel',
            function () {
                var MenuMainModel = {};

                // TODO: complete

                return MenuMainModel;
            }
        );
    }
);

/* EOF */