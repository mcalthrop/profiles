/* global define */

define(
    [
        'angular'
    ],
    function ProfilesModel(angular) {
        'use strict';

        return angular.module('ProfilesModel', []).factory(
            'ProfilesModel',
            function () {
                var ProfilesModel = {};

                // TODO: complete

                return ProfilesModel;
            }
        );
    }
);

/* EOF */