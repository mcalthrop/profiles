/* global define */

define(
    [
        'angular'
    ],
    function AboutControllers(angular) {
        'use strict';

        return angular.module(
            'AboutControllers',
            [
                'AboutController'
            ]
        );
    }
);

/* EOF */