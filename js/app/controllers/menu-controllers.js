/* global define */

define(
    [
        'angular'
    ],
    function MenuControllers(angular) {
        'use strict';

        return angular.module('MenuControllers',
            [
                'MenuMainController'
            ]
        );
    }
);

/* EOF */