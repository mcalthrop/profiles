/* global define */

define(
    [
        'angular'
    ],
    function AboutModels(angular) {
        'use strict';

        return angular.module('AboutModels', []).factory(
            'AboutPageModel',
            function () {
                var AboutPageModel = {};

                // TODO: complete

                return AboutPageModel;
            }
        );
    }
);

/* EOF */