/* global define */

define(
    [
        'angular'
    ],
    function AboutModels(angular) {
        'use strict';

        return angular.module('AboutModels', []).factory(
            'AboutModel',
            function () {
                var AboutModel = {};

                // TODO: complete

                return AboutModel;
            }
        );
    }
);

/* EOF */