/* global define */

define(
    [
        'angular'
    ],
    function AboutModels(angular) {
        'use strict';

        return angular.module(
            'AboutModels',
            [
                'AboutModel'
            ]
        );
    }
);

/* EOF */