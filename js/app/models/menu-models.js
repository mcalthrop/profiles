/* global define */

define(
    [
        'angular'
    ],
    function MenuModels(angular) {
        'use strict';

        return angular.module('MenuModels', []).factory(
            'MenuMainModel',
            function () {
                var MenuMainModel = {};

                // TODO: complete

                return MenuMainModel;
            }
        );
    }
);

/* EOF */