/* global define */

define(
    [
        'angular'
    ],
    function PageFooterModel(angular) {
        'use strict';

        return angular.module('PageFooterModel', []).factory(
            'PageFooterModel',
            function () {
                var PageFooterModel = {};

                // TODO: complete

                return PageFooterModel;
            }
        );
    }
);

/* EOF */