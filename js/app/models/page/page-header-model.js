/* global define */

define(
    [
        'angular'
    ],
    function PageHeaderModel(angular) {
        'use strict';

        return angular.module('PageHeaderModel', []).factory(
            'PageHeaderModel',
            function () {
                var PageHeaderModel = {};

                // TODO: complete

                return PageHeaderModel;
            }
        );
    }
);

/* EOF */