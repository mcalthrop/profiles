/* global define */

define(
    [
        'angular'
    ],
    function ProfilesModels(angular) {
        'use strict';

        return angular.module(
            'ProfilesModels',
            [
                'ProfilesModel'
            ]
        );
    }
);

/* EOF */