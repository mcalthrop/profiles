/* global define */

define(
    [
        'angular'
    ],
    function ProfilesModels(angular) {
        'use strict';

        return angular.module('ProfilesModels', []).factory(
            'ProfilesModel',
            function () {
                var ProfilesModel = {};

                // TODO: complete

                return ProfilesModel;
            }
        );
    }
);

/* EOF */