/* global define */

define(
    [
        'angular'
    ],
    function AboutModel(angular) {
        'use strict';

        return angular.module('AboutModel', []).factory(
            'AboutModel',
            function () {
                var AboutModel = {};

                // TODO: complete

                return AboutModel;
            }
        );
    }
);

/* EOF */