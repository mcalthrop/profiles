/* global angular */

angular.module('ProfilesModel', []).factory(
    'ProfilesModel',
    function () {
        var ProfilesModel = {};

        // TODO: complete

        return ProfilesModel;
    }
);

/* EOF */