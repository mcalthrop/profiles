/* global angular */

angular.module(
        'ProfilesModelModule',
        [
        ]
    )
    .factory(
    'ProfilesModel',
    function () {
        var ProfilesModel = {};

        // TODO: complete

        return ProfilesModel;
    }
);

/* EOF */